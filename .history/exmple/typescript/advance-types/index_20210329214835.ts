function strStr(haystack: string, needle: string): number {
  if(haystack.length === 0){
      return 0
  }
  if(haystack.length < needle.length){
      return -1
  }
  let j = haystack.length
  let k = needle.length
  let i = 0
  while(i<j){
      if(i==)
      if(haystack.charAt(i) == needle.charAt)
  }
};