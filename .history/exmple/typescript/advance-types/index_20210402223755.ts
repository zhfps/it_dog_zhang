function addBinary(a: string, b: string): string {
    let al = a.length-1;
    let bl = b.length-1;
    let temp = 0
    let result = ''
    while(al<0 && bl<0 && temp===0){

      let ai = a.charAt(al)
      let bi = ai.charAt(bl)

      if(ai === '1' && bi === '1'){
        
      }

    }

    return result
};